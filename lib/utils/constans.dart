
import 'package:flutter/material.dart';

class AppColors {
  // Button colors
  static const Color primaryButtonColor = Color(0xFF0560FD); // Primary button color
  static const Color secondaryButtonColor = Color(0xFFFFFFFF); // Secondary button color

  // Input field colors
  static const Color inputFieldBorderColor = Color(0xFFDDDDDD); // Border color of input field (inactive)
  static const Color inputFieldFocusColor = Color(0xFF0560FD); // Border color when input is focused
  static const Color inputFieldFillColor = Color(0xFFF6F6F6); // Background fill color of input field

  // Other colors (optional, for general app theming)
  static const Color backgroundColor = Color(0xFFFFFFFF); // Background color of the screen
  static const Color textColor = Color(0xFF000000); // Text color (e.g., for labels)
}
